# ---- 运行时数据（体积大、变动频繁）：不进版本库 ----
data/archive/
data/backups/
data/cache/
data/check_cache*/
data/imports/
data/out/
candidates*.json
final_*.json
auto_added.json
shareBookSource*.json
book_sources.sqlite3
*.ndjson
# 例外：**测试夹具**不是运行时数据。`test_jvm_debug*.py` 拿一份真实产出的 NDJSON
# 钉两侧同构（Kotlin 落盘 ↔ Python 解析），它必须随仓库走——否则干净检出下那 15 条
# 全红，而「同构」这条唯一的钉子在任何别的机器上都不存在（本机绿只是因为文件恰好在）。
!tests/fixtures/*.ndjson
index.json

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uvcache/

# ---- 工具 / 编辑器 ----
.codegraph/
.omo/
.zcode/
.vscode/
.idea/
.claude/
*.log
_probe*.txt
_migrate*.py
_fix*.py
_inspect*.py


# 测试临时目录（仓库内，避免系统 %TEMP% 不可写）
.tmp/

# SQLite 管理库（二进制、变动频繁）
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# LLM 本地配置（含 API Key，禁止提交）
data/config/
# 实测产物目录（设备探针 + JVM 调试/跑批 + 常驻日志）。**仍在写**：
# core/jvm_debug.py、core/jvm_daemon.py、backend/api/jvm.py、
# scripts/jvm_debug_run.py、scripts/jvm_debug_direct.py、tools/probe_app_debug.py。
data/app_probe
