# 待办与二期候选

> **分工**：本文件只放**要做什么**（动作 + 够动手用的依据）。可复用的机制与踩坑
> 写进 `skills/legado-source-lessons`，这里只留一行指针；**已完成的事项从这里删掉**，
> 别让它在这儿过期（这份文件反复订正过，根因就是分工没定）。
>
> ⚠️ 行号是 **2026-09-16 的快照**，代码一改就会漂，引用前先核对。
>
> **最近已修**（2026-09-16 ~ 09-17；机制见 `skills/legado-source-lessons`
> §十八/§十九/§二十七～§三十五，**细节看 `git log`，这里不再逐项列**）：
> 并发下的两处随机 500（SQLite 连接跨线程 / 每个请求重建 schema）、后端直接托管
> 前端产物、探测深度合并成一根四档轴、删除备份改单文件 JSONL、`证书问题` 进系统
> 标签表、reclassify 的证书分档、名称清洗全链路、"合并重复源"的 P0/P1、
> 校验链路的三个「崩溃/失明」问题（fd 耗尽、进度卡住、超时与异常让源永远显示
> 「未校验」）、DNS 失败归因（域名注销 vs 解析被污染）、证书错误单独分档、
> 任务僵尸行与 `checks` 保留策略、CLI 归因与校验链路对齐、新增 `dups` 找重复源。
>
> 更早那批（类型判定自固化、`@html:` 前缀、`webView` / `enabledExplore` 的推导与存量
> 清理、`CACHE_VERSION` 8→9 等）同样以 lessons 与 git 历史为准——**这份清单只用来
> 回答"这条是不是刚做过"，不承载细节**（它原来每轮都在长，正是本节开头警告的那种
> 过期）。
>
> **已定决策：缓存不做向后兼容。** 它是纯派生数据，口径一变就整体作废
> （`CACHE_VERSION`）。做兼容反而危险——旧缓存里的 health 是旧逻辑的产物，混着用
> 就是「修了等于没修」（v7、v9 两次都是这个理由）。代价是每次口径变化要一次全量
> 重跑（约 3850 条），已知且接受。

---

## 明确该做，需排期

### 1. ⚠️ **前提已过时，需重写**：本地回放不带 cookie

**原稿写的是「试跑不带 cookie」——那个前提已经不存在了。** 编辑弹窗的调试
**只剩「连 App」一条路**（本地离线试跑入口已撤掉），而调试跑在 App 里，
cookie 由 **App 自己的 CookieJar** 管，**我们不需要碰**。

本地 `verify_chain` 仍在跑的只剩三处：

| 路径 | 需要 cookie 吗 |
|---|---|
| 快速生成（`services/add_source.py`） | 新建源，本来就没有登录态——**不需要** |
| AI 修复循环（`core/repair/loop.py`） | **可能**需要：修的可能是带登录态的旧源 |
| `/api/rules/chain`（规则试跑） | 同上传入的 source，取决于调用方 |

**所以要重写成它真正的样子**：不是「试跑」的问题，是「**本地回放（修复循环）遇到
登录墙**」的问题。而修复循环本来就用 App 之外的能力，那才是值得讨论的地方。

**在此之前不要动手**——先确认修复循环实际会不会撞上登录墙（可以查它处理的源里
有多少带 `loginUrl`）。

> 顺带记一笔：这一条是**前提腐烂**的典型——需求写在能力还在的时候，能力撤了，
> 条目还留着。定期按「这条现在还能触发吗」过一遍，比按时间过一遍有用。

### 2. ⚠️ **P0/P1 已落地，只剩 P2**：「合并重复源」

**已实现**（2026-09-17）：`整理源` 抽屉（`TidyDrawer.vue`）第 1 步名称清洗、第 2 步
重复梳理与合并；后端 `GET /api/sources/dups`、`POST /api/sources/merge`（含 `dry_run`）、
`/merge/undo`；编排在 `services/merge_sources.py`（判据在后端**重算**，不信前端分组）。

**实测规模**（管理库，2026-09-17）：可合并 **369 组 / 782 条 / 可精简 413 条**
（同站点 + 行为指纹相同）。只读的三档：镜像 62 组、同域名 885 组、同名 407 组 ——
它们**只展示不动作**，`m.suixkan.com` 那种一个域名下 5 种源就是要人判断的例子。

**还没做**（P2 智能）：① B/C 档并排显示可用性辅助判断留哪条；② 「一键处理所有 A 档」
按建议批量应用；③ 「都留着并记住」（需要持久化，见「已定的决策」第 1 条）；
④ **导出时附一份「本轮合并掉的地址清单」**——App 的导入是按地址覆盖，被合并掉的旧
地址不会自己消失，用户得照着在 App 里删。

**原始设计稿**（保留备查，交互与判据都已按它实现）：
CLI 已有只读清单（`python -m cli.main dups`，判据与理由写在
`core/dups.py` 开头），但**只能看不能动手**；而"同域名但规则不同""同名但域名不同"
这两类必须人判断——`m.suixkan.com` 一个域名下就有 5 种源（「随心看吧」4 条重复 +
「阅友小说」2 种 + 「随心看」1 条），**按域名自动合并会误删**。

价值：重复条目导出到 App 后，同一本书会出现多个来源、搜索变慢；合并是把书库收拾干净
最直接的一步。

**三档判据**（风险从低到高；**默认勾选只给 A 档**）：

| 档 | 判据 | 现状 | 说明 |
|---|---|---|---|
| **A** 规则完全相同 | `core/dups.rule_signature` 相同（只差地址/署名/排序/备注/分组） | 110 组 / 228 条 | 内容一字不差，可预勾选"合并" |
| **B** 同域名、规则不同 | 域名相同（`core/dups.host_of`） | 同站的不同实现 | 只展示，人判断 |
| **C** 同名、域名不同 | 名称相同 | 镜像站 / 重名站点 | 只展示，人判断 |

**交互（P0 只读 → P1 合并 → P2 智能）**

- **P0 只读**：列表工具栏加「重复源」按钮 → 抽屉（形状照 `JobsDrawer.vue`：主列表的
  工具栏已经会换行，不再往里塞控件）。A/B/C 三个 Tab，每组一张卡片，成员行显示
  地址 / 名称 / 健康度 / 星级 / 备注 / 校验时间，标出「建议保留」（复用
  `core/dups._keep_rank`：**能用 > 地址干净 > 导入早**），加一个「在主列表里看这几条」
  （把成员地址塞进现有筛选）。**不改数据**。
- **P1 合并**：卡片内每条一个单选 = 设为保留，其余自动标"将合并删除"；底部固定条
  `保留 1 条 · 合并删除 3 条` + 两个选项：**☑ 并过被删条的用户标签（默认开）**、
  **☐ 备注一并保留（默认关）**+「应用」。确认框要写清三件事：**软删除**、回收站可恢复、
  备份已写入 `data/backups/deleted.jsonl`。支持跨卡片累积（一次处理多组），每组可「跳过」
  （**只在本次会话有效**，P1 不做持久化）。
- **P2 智能**：① B/C 档并排显示可用性（健康度/星级/上次响应耗时）辅助判断留哪条；
  ② 「一键处理所有 A 档」按建议批量应用；③ 「都留着并记住」——需要持久化，
  见下面「已定的决策」第 1 条。

**「合并」不只是删除**，保留的那条要吸收被删条目的东西：

1. **用户标签取并集** → `POST /api/sources/tags` 的 `add`（它的语义是"加标签、绝不覆盖
   其他标签"，正好合适）
2. **备注**：**默认不合并**，但组内备注不一致时在卡片上显式标出来、两边内容并排显示
   （用户勾了才写回保留条的 `bookSourceComment`，走 `POST /api/sources/save`）。
   依据：110 个重复组里 **88 组备注全空、约 14 组相同、只有 8 组不一致**——拼接在 92%
   的情况下毫无作用，而在那 8 组里会把「JS 片段 / 分享者签名 / 同站对照表」糊成一段。
   默认不勾 = 程序不悄悄改写用户手写内容（与 `organizer._clean_comment` 的克制一致）
3. 其余走 `POST /api/sources/delete`，`reason` 写「合并到 <保留的地址>」——回收站里
   能看出为什么被删；**不需要新增备份机制**，这个接口本来就把整条 `raw_json` 追加到
   `data/backups/deleted.jsonl`
4. **撤销（P1 必做，不是可选项）**：它把"合并"从不可逆变成可逆，用户才敢批量应用；
   否则每合一组都得先想三秒。三件事都要还原，接口得把够用的信息返回：
   - `POST /api/sources/restore` 恢复被删的那几条
   - `tags_added` 移除标签——**必须是"本次真正新增"的**（先算差集再返回）。被删条身上的
     标签可能本来就在保留条上，原样回传会**删掉用户本来就有的标签**（标签还走别名归一，
     见 `core/tags.normalize_user_tags`）
   - `prev_comment` 还原保留条的备注（只在用户勾了备注合并时才有值）
   - **入口做成"应用后的结果条"**（复用 `checkResult` 那条持久条的形态），刷新即失效——
     不要做成回收站里的常驻按钮：几天后再点一次撤销，会把当时并过来的标签从保留条上摘掉，
     而那时它可能已经有别的用途

**接口草案**（判据全部复用 `core/dups`，**不在前端重算**）：

- `GET /api/sources/dups?kinds=rules,host,name` →
  `{summary, groups: [{kind, hosts, members: [...], suggest_keep}]}`
  （`core/dups` 目前只有"规则相同"一种分组，要补 `by_host` / `by_name` 两个入口）
- `POST /api/sources/merge`，body `{keep, drop: [...], merge_tags, merge_comment}` →
  `{merged, tags_added, prev_comment, restore_urls}`（`tags_added` 只装**真正新增**的
  标签、`prev_comment` 只在勾了备注合并时才有值，两者都是给撤销用的）
- **先支持干跑**（`dry_run=true` 返回"将发生的三件事"的清单：哪些条会被软删、
  哪些标签会被加上、备注会不会被改），确认框直接拿它渲染
- 撤销复用 `POST /api/sources/restore` + `POST /api/sources/tags` 的 `remove`

**明确不做**：

- **不自动合并**（A 档也不）：删源是不可逆的资产变更，一律"给建议 + 人点确认"
- **不按域名批量去重**：见上面 `m.suixkan.com` 的反例
- **不改地址**（比如把带署名的统一成不带）：地址是 Legado 的源身份，改了在 App 里
  等于**换了一条源**（导入是"按地址覆盖"，旧的那条不会消失）
- **不做逐组向导**：110 组一组一问太慢，卡片 + 批量勾选够用
- **不把"忽略/已处理"放进设置**：`POST /api/settings/reset`（恢复默认）会把它们**静默
  清掉**；这类"用户对数据的决定"在本项目里一律存库（`user_tags`、`system_tags_locked`）

**已定的决策**（评审后收敛，实现时别再重新纠结）：

1. **P1 不做"忽略持久化"**——"跳过这一组"只在本次会话有效就够（清单是幂等的，刷新
   重来无所谓）。P2 要做时用**一张表**装两种决定，别塞 settings：
   `dup_decisions(kind, key, decision, note, created_at)`，`decision ∈ {ignored, merged}`
   ——"忽略这组"和"这组合并过"形状一样（都是对某一组做了个决定），一张表既解决忽略，
   又顺手给出"哪些组处理过、什么时候"
2. **备注默认不合并**（依据见上面第 2 条），只在组内不一致时显式提示
3. **撤销放 P1**，入口是"应用后的结果条"（刷新失效），且只回滚**真正新增**的标签

**验收**：分组数与 CLI 清单一致（同一份 `core/dups`）；合并后保留条的标签是并集、
被删条在回收站且 `reason` 写明、可恢复；**撤销后回到合并前的状态**（标签与备注都不留
残余）；`dry_run` 与实际执行的条数一致。

### 3. 试跑页面缓存复用：把「改一次选择器试一次」压到毫秒级

**要解决的问题**：调试的反馈环是**重新联网**。实测（管理库 9015 条响应）：单页
p50 **805ms**、p90 3.1s；一条链抓 3 页（搜索 / 详情 / 正文，目录复用详情页，
`app_debug.MAX_PAGES`）→ 一轮 2.4 秒上下。而**解析本身是毫秒级**。

**设计上最有利的一点**：`core/fetch.fetch` 是**唯一的同步抓取出口** —— `analyzer`、
App 调试的补抓（`fetch_debug_pages`）、`verify_chain`、AI 修复的 `evidence`、
`services/add_source` 全走它。所以缓存**只加在这一个函数里**，所有「本地抓页面」
的链路自动受益，不必逐个改。

**做法**

1. 在 `core/fetch.py` 加进程内页面缓存：
   `{(method, 归一化URL, header摘要, body摘要) → (html, charset, fetched_at)}`。
   - **键必须含请求身份**（header/charset/body）。登录态或 UA 不同抓到的页面不同，
     只按 URL 做键会把两种身份的内容串在一起（同一类坑见 lessons §五）。
   - TTL **默认 5 分钟**：这是「页面缓存」，与「结论缓存」（`cache_ttl_ok/other`，
     7/14 天）是两件事，名字与量级都要分开，别让人以为调了它就能少校验。
   - 容量：默认 200 页、单页 1MB 截断（超出标记「已截断」），LRU 逐出。
   - **只缓存 2xx**：失败页缓存下来会让「站点恢复了」看不见。
   - 不落盘（重启即失效，调试场景够用）。
2. **一个开关：「只重解析不重抓」**（调试抽屉里，per-run）。置位时**完全不发请求**，
   缓存缺失/过期就明确报「这一页没抓过」——**绝不偷偷去抓**，否则用户以为在看缓存，
   实际又联网了。
3. **缓存必须可见**：每条 page 标出「抓取时间 + 是否来自缓存」，并给「重新抓取」。
   默认打开缓存时，这个标注就是「你看到的不是我刚抓的」的唯一线索。
4. **能力边界不变，要说清楚**：缓存的是**页面**，不是**结论**。`<js>` / `@js:` 这类
   规则本地仍然跑不了 —— 它省掉的是重复联网，**不改变回放能力**。所以「连 App 调试」
   那条链的 JS 部分依旧只能靠 App；别让人把"快了"当成"能验了"。

**收益**：单页 p50 805ms → 解析毫秒级；一条 3 页的链 2.4s → ~10ms，**两个数量级**。
最大的受益者其实是 **AI 修复循环**（`core/repair/loop.py`）：证据只抓一次，但**每一轮
都要重跑 `verify_chain`，而它会重新抓那几页** —— 多轮下来成倍。

**不做（v1）**：条件请求（ETag / Last-Modified）——那才是「既新鲜又不重传」的正解，
但要自己拼请求头、判 304，先把缓存本身做出来；写在这里备查。

**与「调试时写入验证缓存」的区别**（P2 那一节）：那条是把调试**结论**写进结论缓存，
前提是「只有已保存的源才有意义」（要比 fingerprint）；本条缓存的是**页面**，
不需要源已保存，也不写任何结论。

## 待决策（要产品判断，不是实现活）

> 这两条是**审阅时发现的**：它们原先挂在某个待办条目下，而那个条目做完了就被删掉，
> **问题本身却还开着**——于是引用变成了悬空指针（`core/store.py` 和
> `tests/test_checker_judge.py` 里各有一处）。**做完一件事 ≠ 顺便回答了它牵出的问题。**

### 1. ✅ 已决（2026-09-17）：导入外部源的陌生标签**不收**

白名单制：`Store.known_user_tags()` = `core.tags.DEFAULT_USER_TAGS`（R18 / 正版）
+ 库里已有的用户标签。导入接口与 `upsert_sources` 两道用**同一份**名单；
从界面保存的源不受它管（那是用户明确勾的标签，`save_source` 紧跟 `set_user_tags`
覆盖是对的）。

### 2. 星级要不要收紧：没实测的维度不该拿到对应星？

全量校验后的实测（**2026-09-17 复核过一遍**，`v_sources` 每源最近一条，
排除了 204 条 star_basis 为空的历史行——那一列是后来 ALTER 补的，09-02~09-09
写下的行没有它）：

    5★ static     212 条   ← **5★ 的含义是「正文可用」，而这些一条正文都没验过**
    4★ measured     1 条
    3★ static    1612 条   ← 未命中测试作品，但目录+正文规则齐全
    3★ measured   229 条
    2★ measured    32 条
    1★ measured  1126 条
    0★           444 条   ← 超时/异常/失效/被墙（瞬时结论现在也会落库，见 lessons §二十九）

> ⚠️ 本节原稿列的 314 / 979 / 1503 与现在的库对不上（那是更早一次全量后的快照，
> 之后跑过校验，星级整体上移）。**拿旧数字做决策会跑偏**，要动之前先重算一次。

`calc_stars` 的既定行为是「None → 回退静态规则」。标上「仅规则」只是**如实呈现**；
**要不要连星级本身也收紧**（未验证的维度不给对应星）是产品决策。

- 会改这 **314 条**的星级
- 属**重大口径变化**：要配 `CACHE_VERSION` + 一次全量重跑
- 反方理由：`3★ static` 有 979 条，而那些是「没命中测试作品」——按既定原则
  **未命中 ≠ 源差**，不该因为「没验」就掉星
- **还有一半没答**：0★ 那 1503 条里绝大多数是"这次没测通"（超时/异常/被墙），
  把它们和"确实死了"压在同一个 0★ 上，与本节问的是同一件事——星级没有区分
  「没验」和「验了不行」。真要收紧，两个方向应该一起定

见 `tests/test_checker_judge.py` 的 `StarBasisTests`。

---

## P2 · 体验类，按需

> 「试跑页面缓存复用」原本在这一节，已升格成正式的「明确该做」第 3 条（见上）。

- **前端暴露 `pick`**：搜索结果多条时选第 N 条重试（后端 `pick` 参数已支持；
  Legado 固定取第 0 条，见 `Debug.kt:285-288`）。
- **探索步骤（Explore）**：补齐 Legado 的第五个调试目标。
- 点击摘要里的变化数 → 按对应筛选跳转（需与统计条 chip 的筛选状态协同）
- 校验历史时间线（每次校验的前后对比，而不只是聚合数）——
  **依赖放宽 `checks` 的保留策略**：现在每源只留最近一条，时间线至少要留 N>1 条
  （改一个数字的事，但得先定 N 定多大、以及它带来的库增长），见 lessons §二十八
- **跨页保持勾选**：本次用显式 URL 列表绕开了，但「我选了这几条、翻页后又想加几条」
  的场景仍在
- `TrashDrawer` 的批量恢复对齐同一套 URL 语义
- 元素点选生成规则（iframe 渲染 + 点选元素反推 Legado 规则串）

### 可考虑：调试时写入验证缓存

方向可行，但有个前提：**试跑的是表单里当前的规则**，未保存时 `fingerprint` 与库里
不一致，缓存写了也用不上（`is_cache_item_valid` 要比 fingerprint）。所以只对
**已保存的源**有意义。口径上没问题——项目已做「判定收拢到 `core.quality`」。
时效已有：`cache_ttl_ok`(14天) / `cache_ttl_other`(7天)，**已在设置里可改**
（键在 `core/settings_store.DEFAULTS["check"]`；这里不写行号——它每轮都漂）。

---

## 需先调研（**不要直接动手**）

### A. 书源类型判定的判据改造

现状的六个问题：

| # | 问题 | 位置 |
|---|---|---|
| ① | **数学上判不出 1（音频）和 3（下载源）**——`infer_type_static` 只有 `2`/`0`/`-1` 三个出口 | `core/reclassify.py:121-127` |
| ④ | 已声明的 1 / 3 被完全无视——只处理 `declared == 2` 和 `declared == 0` | `core/reclassify.py:116-120` |
| ⑤ | `-1`（证据不足）的语义是「保持原样」，而多数源本来就是默认 `0` → **"保持原样"就是保持错误**；且没有任何路径能产出 `4`（❓未知） | `core/reclassify.py:127` |
| ⑥ | 根本问题：`bookSourceType` 决定的是**阅读器走哪条渲染路径**（`BookSourceType.kt` 的 `@IntDef` + `BookSourceExtensions.getBookType()`），而现有判据全是「域名/名称像什么」——**用内容线索猜渲染路径** | `core/reclassify.py:34-50` |

要做的两件事（**都需先调研**）：

| # | 推迟项 | 依据 | 卡在哪 |
|---|---|---|---|
| 1 | 补齐音频 / 下载源的静态出口 | ①④⑤ | 要产出 `1`/`3` 得先有信号 |
| 2 | 判据从「域名/名称」改为「**规则形状**」 | ⑥ | 同上，且见下面的实测 |

**第 2 项才是真正的解法**，思路是对齐 Legado 的 `Debug.kt:329-332`——用
`book.isWebFile`（由 `downloadUrls` 存在决定，`BookExtensions.kt:94`）判下载源，
**不是猜的**。

> ⚠️ **但这条思路现在搬不过来**：实测**管理库**（3861 条，2026-09-17 复测；
> 原来引用的 `data/candidates.json` 已经不存在了）里，**带 `downloadUrls` 的是 0 条**。
> 所以两项都必须**先做一次调研**：摸清 3861 条里到底存在哪些**能定案**的结构信号，
> 再谈判据换不换。
> **在此之前不要动 `infer_type_static` 的出口**——没有信号就加出口，只会把
> 「猜域名」换成「猜别的东西」。

`reclassify --write` 在那之前仍只能在 `0` / `2` 之间翻转。
推迟的代价：`organizer.group_title` 与 `store._system_group_for` 都用
`BOOK_SOURCE_TYPE_NAMES` 生成类型标签，所以类型判定不补齐，App 里的类型分类就会
一直带着 ①④⑤ 的偏差。当时提供的是「人主动纠正单条源」的通道，不是批量修正。

### B. **取值类**规则的末段语义差异（**290 条**，已定案）

**一句话**：Legado 对**列表类**规则和**取值类**规则用**两个不同的函数**，而我们对两者
用同一套解析器：

| 规则种类 | Legado 走 | 末段怎么解释 | 我们 |
|---|---|---|---|
| 列表（`chapterList` / `bookList`） | `getElements`（`BookChapterList.kt:203`） | **每个 @ 段都是选择器** | 一致 ✓ |
| 取值（`content` / `intro` / `author`…） | `getStringList` → `getResultList` → `getResultLast` | `when` 只认 text/textNodes/ownText/html/all，**其余当属性名**（`attr()`） | 当选择器 ✗ |

所以 `content: "id.content"` 这类「末段写裸选择器」的**取值**规则，Legado 取的是
`attr("id.content")` → **空**，而我们给出正文 → **误放**。

**实测范围**（全库 3861 条）：

    落在列表类字段 → 3151 条  **我们是对的，销案**
    落在取值类字段 →  290 条  ← 真正的对象
      content.content 41 / search.coverUrl 40 / bookinfo.author 38 /
      bookinfo.intro 22 / bookinfo.kind 20 / bookinfo.name 18 / …

> **2026-09-16 的两次订正，值得记下来**：
> ① 最初写成「3441 条」，是把列表类字段也算进去了——**没分清两个函数**。
> ② 中间一度被 App 实测（`chapterList = class.chapter-list@tag.a` **能出章节**）
>    推翻，降级成「待实测」。后来才发现那条走的是 `getElements`——
>    **反例与我读的函数不是同一个**，两件事都对。
>
> 教训：**「实测推翻了源码结论」时，先确认两边说的是不是同一个代码路径。**

**动作**：只在**取值类**字段上改（列表类一个字都不能动）。
**必须灰度**：会让这 290 条的对应字段由「有值」变「空」，可能连带影响星级。

### C. `ruleContent` 里我们**完全没建模**的字段

App 用它们改写正文（去广告、拼副文、二次解密图片），**我们一条都不用**——
这比 `webView` 更直接地构成「同源不同判」。

**2026-09-16 逐个核过源码，全部在用**（引用补全）：

| 字段 | App 在哪用 | 干什么 |
|---|---|---|
| `replaceRegex` | `BookContent.kt:190` | 正文替换 |
| `subContent` | `BookContent.kt:144` | 副文规则，拼在正文后（或取歌词） |
| `nextContentUrl` | `BookContent.kt:254` | 下一页（**这个我们已建模**） |
| `sourceRegex` | `WebBook.kt:430` → `AnalyzeUrl.kt` | 页面源码正则（WebView 分支） |
| `callBackJs` | `SourceCallBack.kt:53,92` | 事件回调 JS |
| `payAction` | `MangaReaderActionRepository.kt:237-244` | 漫画购买操作（evalJS） |
| `imageDecode` | `BookHelp.kt:367-389` | 图片 bytes 二次解密 |

**实测使用率**（2026-09-16，全库 3861 条）：

| 字段 | 非空 | 说明 |
|---|---|---|
| **`replaceRegex`** | **1024 条（26.5%）** | App 在正文提取**之后**做全文替换：`analyzeRule.getString(replaceRegex, contentStr)`（`BookContent.kt:191`），走的是**规则引擎**不是裸正则 |
| `title` | 41（1.1%） | 有些站只能在正文里取标题 |
| `sourceRegex` | 19（0.5%） | 键出现 667 次，绝大多数是空串 |
| `payAction` | 13（0.3%） | |
| `imageDecode` | 11（0.3%） | |
| `callBackJs` | 1 | |
| `subContent` | **0** | |

**优先看 `replaceRegex`**：26.5% 的覆盖面，但**风险方向单一**——我们判「非空即通过」，
只有「替换后变空」才会构成误放（替换多是去广告，正文仍在）。所以大概率是
**低风险、高覆盖**，值得评估但不必恐慌。其余几个都是个位数，按需。

**未评估前不要动手。**

---

> **已明确不做的项**（附当时砍掉的理由）在 `skills/legado-source-lessons` §二十六——
> 那是**决策记录**，不是待办。放这里会和「还没做」混成一片。
